{
  "name": "publicidad",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build:cloudflare --prefix frontend",
    "deploy": "npm run build && wrangler deploy",
    "pages:deploy": "wrangler pages deploy dist --project-name=publicidad"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
